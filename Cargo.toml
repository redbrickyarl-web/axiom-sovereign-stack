[package]
name = "axiom-sovereign-stack"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[features]
default = []
aethelarch = []

[dependencies]
crossbeam-queue = "0.3"
crossbeam-epoch = "0.9"
parking_lot = "0.12"
simd-json = "0.13"
tokio = { version = "1.0", features = ["full"] }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "performance_bench"
harness = false
